# 2026-07-18

## SilverShield Home 感知模块 — 工程基础设施设计完成

- 角色：本仓库负责 SilverShield（老年人诈骗风险数字孪生与协同预警系统）的 **Home 感知模块**，owner = 网络工程。
- 已对齐《银龄盾_架构设计完善版(1).docx》V2 终稿：本模块 = **Perceive 逻辑模块 + 门前时空异常与蹲守识别子系统**，部署 Home 端。
- 关键边界（必须遵守）：只输出 5 类标签事件（visit_normal / visit_pending_verify / abnormal_dwell / repeat_visit / high_risk_approach），**不直接判定"诈骗人员"**；风险引擎 Rule+ML 两层，LLM 解释推迟 v2；隐私触发式取证。
- 产出：目录树 `src/home_perception/{core,ingestion,detection,analysis,evidence,output,common}` + config/docs/tests/scripts/data；契约代码已落地（config 加载、事件模型、日志、EZVIZClient、FrameSource 重连、OddHourRule、CooldownGate）；检测/取证/上报为占位 stub。
- 验证：7 个契约测试通过（venv: ~/.workbuddy/binaries/python/envs/ss_home）。
- 文档：docs/00-09（定位/架构/目录/规范/Git/契约/事件Schema/Roadmap/风险）。Git 轻量 GitFlow（main + feature/*，MVP 可简化），已初始提交。
- 下一步 Phase1：YOLO 检测 → 门前规则(Dwell/Repeat/Pending/HighRisk) → 取证 → MQTT 上报 → main 装配；详见 docs/08_roadmap.md 的 P0-1~P0-7。

## 注意
- 原 test.py/test_stream.py（含真实 APP_KEY/SECRET/序列号）已移入 prototypes/ 并 gitignore，禁止提交。
- 萤石流：已验证 HLS（protocol=2）延迟高；MVP 改用 RTSP（protocol=1）优先，HLS 回退。
- `.doc/` 研究草稿已 gitignore（仅本地参考），终稿结论沉淀在 docs/。
