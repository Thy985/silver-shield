# ADR-0033 Benchmark 回归门禁专用约束（与 production requirements.txt 解耦）。
#
# 作用：钉死 regression 比较所依赖的关键数值/AI 库精确版本，使 committed baseline
# （src/home_perception/evaluation/fixtures/baselines/adr0033-phase1.json）可重放、
# 跨时间 / 跨 OS 稳定。这正是 benchmark 最核心的工程原则：baseline 必须可重放。
#
# 与 production 的关系：
#   - requirements.txt / requirements-dev.txt 保持"下限灵活"（>=），面向日常开发；
#   - 本文件只约束 benchmark 回归门禁比较相关的依赖，不污染生产依赖解析。
#
# 升级流程（D7 baseline bump 治理）：
#   1. 在 PR 中同步升级本文件版本；
#   2. PR 标注 benchmark-baseline-bump（Owner 评审）；
#   3. 用升级后的环境重新 capture baseline 并提交（CI e2e --gate 会校验守恒）。
#
# 版本必须与 committed baseline 的 provenance.runtime_dependencies 完全一致：
#   本地 CUDA 构建的 torch 2.11.0+cu130 经 harness.normalize_version 归一为 2.11.0，
#   与 linux CPU 构建的 2.11.0+cpu 归一结果相同 → 跨 OS 可比。
numpy==2.4.2
opencv-python==4.13.0.92
torch==2.11.0
